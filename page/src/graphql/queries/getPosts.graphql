query GetPosts($sort: String, $clique: String) {
  getPosts(sort: $sort, clique: $clique) {
    id
    title
    content
    posterID
    upvoteCount
    downvoteCount
    viewCount
    createdAt
    updatedAt
    postType
    postClique
  }
}
