mutation Register($firstName: String!, $lastName: String!, $userName: String!, $password: String!) {
  register(userID: $userName, password: $password, firstName: $firstName, lastName: $lastName) {
    status
    message
    user {
      id
      userID
    }
  }
}
