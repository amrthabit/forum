mutation CreatePost(
  $title: String!
  $content: String!
  $posterID: Float!
  $postType: String!
  $clique: String
) {
  createPost(
    title: $title
    content: $content
    posterID: $posterID
    postType: $postType
    clique: $clique
  ) {
    id
  }
}
