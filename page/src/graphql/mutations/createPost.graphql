mutation CreatePost(
  $title: String!
  $content: String!
  $posterID: Float!
  $postType: String!
) {
  createPost(
    title: $title
    content: $content
    posterID: $posterID
    postType: $postType
  ) {
    id
  }
}
